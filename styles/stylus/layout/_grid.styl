.grid
  display flex
  flex-wrap wrap
  position relative
  justify-content flex-start
  flex-direction row
  [classe^="grid--item"]
    flex-shrink 0
    margin-right 12px
    flex-grow 1
  for i in grid-size
    & &--item-{i}
      width (100 / i)%